# v2.7.0 Release Plan — Phase 2C+ close-out

> Status: **draft** (2026-04-20). Tag발행 조건 = Codex LATTE-on 14/14 reviewer lane 공식 측정 완료 + `scripts/release_gate.sh` 결과 release note 인용.

## Scope

v2.7.0은 **Phase 2C+ close-out tag**입니다. Pivot 2026-04-19가 예고한 세 가지 재측정 결과 시나리오 중, 5-pair preliminary 결과 기준으로 **시나리오 C ("변동 없음 → 2C+ 전략 재검토")**에 해당하는 것이 확인된 상태에서의 공식 릴리즈입니다. 이 tag는 "detection 보강 4-6주 작업이 끝나서 multi-agent로 넘어간다"는 식의 승리 서사가 아니라, **"2C+ 4항목을 실측했고, Gate 1/3은 2C+ 내부 작업으로 도달 불가임을 정직하게 기록한다"**는 close-out입니다.

## Included changes (CHANGELOG `[Unreleased]` → `[2.7.0]`)

### Added
- `compliance_report` stage (Phase 3'.1 step B-4) — 43번째 pipeline stage
- LATTE-inspired backward slicing (Phase 2C+.1) — opt-in via `AIEDGE_LATTE_SLICING=1`
- LARA URI/CGI/config-key source identification (Phase 2C+.2)
- Sink coverage 28 → 51+ with format-string variable detection (Phase 2C+.3)
- Finding diversity gate + pair-eval timeout diagnostic (Phase 2C+.5)
- FDA Section 524B compatibility mapping (Phase 3'.1 step B-2)
- ISO/SAE 21434 compatibility mapping (Phase 3'.1 step B-3)
- UN R155 compatibility mapping (Phase 3'.1 step B-3)

### Changed
- CRA mapping relocated into `docs/compliance_mapping/` (Phase 3'.1 step B-1)

### Fixed
- **LARA `ascii_strings` wire-through** (Phase 2C+.2 follow-up) — 2C+.2의 초기 landing이 enhanced_source.py 호출부에서 `ascii_strings` 파라미터를 전달하지 않아 URI/CGI 매칭이 0으로 degenerate된 것을 수정. 실측 BEFORE 0 → AFTER 10-33 matches per firmware.
- AFL++ Docker fuzzing artifact ownership (PR #7)
- Fuzzing stage status when AFL++ never executes the target (PR #8)

### Reviewer lane instrumentation (scripts 신규, code 변경 아님)
- `scripts/run_reviewer_sequential.sh`
- `scripts/run_reviewer_codex_latte.sh`
- `scripts/run_reviewer_claude_latte.sh`
- `scripts/watch_codex_then_latte.sh`

## Reviewer Lane 공식 측정 결과 (1차)

> 완주 후 실측으로 교체. Preliminary 5-pair 기준 수치는 시나리오 C 판정 근거 제공 목적.

| Gate | 임계값 | Summary-Reuse Baseline (7-pair) | Preliminary Codex LATTE-on (5-pair) | 판정 |
|------|--------|---------------------------------|-------------------------------------|------|
| 1 Recall | ≥ 0.40 | 0.1429 | 0.20 | FAIL (여전히 60% 이상 부족) |
| 2 Tier 변별력 | ≥ 2 | 1 (`symbol_only`만) | 1 (동일) | FAIL |
| 3 Diversity | < 0.50 | 1.000 | 1.000 (14/10 전부 `web.exec_sink_overlap`) | FAIL |
| 4 Dedicated rerun | 1 driver success | 0 | Codex 10/10 so far, 14/14 완주 대기 | PASS |
| 5 Corpus ≥ 10 | 10 | 7 | 7 (2C+.4 대기) | FAIL |

결론: **Gate 4 제외 4개 FAIL**. Gate 3 diversity=1.000이 baseline과 완전 동일하다는 점이 결정적. 이는 LARA source 확장(0→21-86 hits/run)이나 LATTE slicing 적용이 **finding ID 수준 다양성에 영향을 주지 못한다**는 구조적 한계를 정량 입증. `findings.py`의 primary-finding 선택 로직이 `aiedge.findings.web.exec_sink_overlap` 합성 finding 하나만 사용하기 때문.

## Release note 권장 문구 (draft)

> v2.7.0 is a close-out release for Phase 2C+ (detection reinforcement) as defined by the 2026-04-19 Direction Pivot. Four of the five 2C+ items (LATTE backward slicing, LARA pattern-based source identification, sink coverage expansion, finding diversity gate) are merged. A reviewer-lane measurement run under these changes shows that Phase 2D Entry Gates 1 and 3 (recall ≥ 0.40, finding diversity index < 0.5) cannot be cleared by the 2C+ workstream alone, because all current vulnerability findings are emitted as a single synthesis-stage finding ID regardless of how many source/sink evidence items the taint layer produces. The pivot roadmap's scenario-C (re-evaluate 2C+ strategy) is hereby entered. See `wiki/projects/scout-direction-pivot-2026-04.md` and `docs/v2.7.0_release_plan.md` for the full rationale.

## Tag 발행 체크리스트

- [ ] Codex LATTE-on lane 14/14 완주 확인 (`benchmark-results/pair-eval-dedicated-local7-codex-6h-r2-latte-on/run_index.json` rows == 14)
- [ ] `scripts/score_pair_corpus.py` 7-pair 기준 결과 수집
- [ ] `scripts/release_gate.sh` 결과 수집 (FAIL 예상, 이유 기록이 목적)
- [ ] CHANGELOG [Unreleased] → [2.7.0] 승격, 날짜 기입
- [ ] `pyproject.toml` 2.6.1 → 2.7.0
- [ ] README.md / README.ko.md "What's new" 섹션 v2.7.0 entry 추가 (Phase 2C+ close-out + 시나리오 C 기록 + 차기 v2.7.1에서 2C+.4 + 3'.1 B-5 번들 예고)
- [ ] `docs/status.md` 현재 상태 업데이트
- [ ] gnosis `wiki/projects/scout.md` / `scout-phase-2c-2d-plan.md` / `scout-direction-pivot-2026-04.md` 갱신
- [ ] `git tag -a v2.7.0 -m "..."` + `git push --tags`
- [ ] GitHub Release 작성 (release note 위 문구 기반)

## v2.7.1 scope (후속)

v2.7.0 이후 1-2주 내 예정:
- 2C+.4 Vendor 포맷별 extraction chain 확장 (5종 — DIR-859 / RT-AC68U / WRT1900ACS / DIR-878 / +1) → corpus 7 → 10+ → Gate 5 해제
- 3'.1 B-5 (v2.7.1 release tag 자체)
- Phase 2D' 진입 조건 재검토 결과 반영 (`wiki/projects/scout-phase-2c-2d-plan.md` Phase 2D 진입 Exit Criteria 섹션 update per 2026-04-20 재검토)
